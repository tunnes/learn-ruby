# In Ruby is possible to repeat an determied number using `times` method see:
10.times.each { puts "Sheep" } 

# Also is possible to use an index provided by `times` method:
10.times.each { | i | puts "Sheep - #{i}" } 