
# In ruby the logical operator is the same of other programing languages..
# But we can replace the operator like `||` to `or`, `&&` to `and`,  `!` to `not` and all the operators..

a = 30
b = 40

puts (a > b) && !(a == b) ? "The `a` variable is heigher then `b`." : "The `a` variable is less then `b`."

